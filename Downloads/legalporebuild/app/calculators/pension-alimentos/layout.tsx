import type React from "react"
// import ProtectedRoute from "@/components/protected-route" // Removed for SEO

export default function CalculadoraPensionesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
